# Optional ML dependencies (Phase 3+)
# Compatible with Python 3.10-3.14

# Phase 3: Anomaly Detection
scikit-learn>=1.5.0
joblib>=1.4.0
pandas>=2.2.0
numpy>=1.26.0

# Phase 4: ML-Based DPI Packet Analyzer
torch>=2.0.0
transformers>=4.30.0

# Optional: Accelerate for faster inference
# accelerate>=0.20.0

# Note: For GPU support, install torch with CUDA:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
